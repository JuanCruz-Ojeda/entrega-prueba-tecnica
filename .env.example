# Copiá este archivo a .env SOLO si necesitás overridear los valores por defecto.
# docker-compose.yml ya usa estos mismos defaults, así que el stack levanta sin .env.
#
# Dentro de la red de compose, Redis es alcanzable por el nombre del servicio.
REDIS_HOST=redis
REDIS_PORT=6379
